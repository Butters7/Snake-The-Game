#include "src/Game/Game.h"

int main(int argc, char **argv) {
    char s = '0';
    int number = 0;
    if(number == (static_cast<int>(s) - 48)) {
        Game game;
        game.start();
    }
    return 0;
}